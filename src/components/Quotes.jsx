import SectionWrapper from './SectionWrapper';

function Quotes() {
  return (
    <SectionWrapper title="Quotes">
      <p>No saved quotes at the moment.</p>
    </SectionWrapper>
  );
}

export default Quotes;
