import SectionWrapper from './SectionWrapper';

function ActivePolicies() {
  return (
    <SectionWrapper title="Active Policies">
      <p>You have 2 active policies.</p>
    </SectionWrapper>
  );
}

export default ActivePolicies;
